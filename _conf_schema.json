{
  "tts_mode": {
    "type": "string",
    "description": "TTS 合成方式",
    "options": ["auto", "provider", "dashscope"],
    "default": "auto",
    "hint": "auto=优先用 AstrBot 已配置的 TTS provider(dashscope_tts等), 没有则用下方 dashscope 直连; provider=只用已配置的 provider; dashscope=只用下方直连配置"
  },
  "dashscope_api_key": {
    "type": "string",
    "description": "DashScope(阿里云百炼)API Key(直连模式用)",
    "default": ""
  },
  "dashscope_model": {
    "type": "string",
    "description": "DashScope TTS 模型: cosyvoice-v1 或 qwen-tts-flash / qwen2.5-tts",
    "default": "cosyvoice-v1"
  },
  "dashscope_voice": {
    "type": "string",
    "description": "默认音色, 如 loongstella(温柔女声)/longxiaochun/longwan(萝莉)/longchen(男声)",
    "default": "loongstella"
  },
  "timeout": {
    "type": "int",
    "description": "合成超时(秒)",
    "default": 30
  }
}
